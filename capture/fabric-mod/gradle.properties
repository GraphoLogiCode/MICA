# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G -Djava.net.preferIPv4Stack=true
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.16.5
loader_version=0.19.3
# Pinned 2026-07-04 (was the floating 1.17-SNAPSHOT, which silently moved to
# requiring a Java 21 build JVM and broke the build): concrete latest release,
# built with the toolchain's JDK 21 (see build-mod.bat).
loom_version=1.17.13

# Mod Properties
mod_version=1.4.0
maven_group=mica.capture

# Dependencies
fabric_api_version=0.42.0+1.16